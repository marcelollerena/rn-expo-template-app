// Let's use this index.tsx component as redirection component.
// example:
// if (user === null ) { router.replace('/log-in') }
// if (user) { router.replace('/home')}

export { default } from "@/features/home/screens/home-screen";
